# @adrift/shared

Reserved for future shared JavaScript utilities, constants, or schema helpers.

Do not place React UI, Express routes, or Swift files here. Product-specific code should stay in:

- `apps/web`
- `apps/api`
- `apps/ios`
